{
    "buildCommand": "cd frontend && DISABLE_ESLINT_PLUGIN=true npm install && DISABLE_ESLINT_PLUGIN=true npm run build",
    "outputDirectory": "frontend/build",
    "framework": "create-react-app",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}