# ================================================
# BACKEND ENVIRONMENT VARIABLES
# ================================================
# Copy this file to .env and update with your actual values
# Location: /backend/.env

# Database Configuration (from Supabase Dashboard → Settings → Database)
DB_HOST=db.xxxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_supabase_database_password_here

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (generate a random string - 32+ characters recommended)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_very_long_and_random_jwt_secret_key_here_at_least_32_characters

# CORS Configuration (comma-separated allowed origins)
ALLOW_ORIGINS=http://localhost:3000,http://localhost:3001,https://your-frontend-vercel-url.vercel.app

# Optional: For production deployment
# TRUST_PROXY=true
